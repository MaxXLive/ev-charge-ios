# API keys — never commit
evmap/evmap/Secrets.plist

# Xcode
evmap/build/
evmap/DerivedData/
**/*.xcuserstate
**/xcuserdata/
**/*.xcscmblueprint
**/*.xccheckout
.DS_Store

# fastlane secrets & build output
evmap/fastlane/.env
evmap/fastlane/*.p8
evmap/fastlane/report.xml
evmap/fastlane/Preview.html
evmap/fastlane/screenshots/
evmap/fastlane/test_output/
evmap/vendor/bundle/
evmap/build/

# Playwright MCP screenshots/snapshots
.playwright-mcp/

# Android reference clone (read-only, not part of this repo)
input/

# Local agent instructions (machine-specific paths, simulator UDID)
CLAUDE.md
